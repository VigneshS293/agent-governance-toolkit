# Copy this file to .env.dev.user and fill in real values.
# .env.dev.user is gitignored and must never be committed -- it holds live secrets.

# Secrets. Keys prefixed with `SECRET_` will be masked in Microsoft 365 Agents Toolkit logs.
SECRET_AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=''
AZURE_OPENAI_DEPLOYMENT_NAME=''
